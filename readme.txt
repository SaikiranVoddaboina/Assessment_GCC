
1.Install libcurl (e.g., sudo apt install libcurl4-openssl-dev on Ubuntu).
2.Replace "your_openweather_api_key" with your actual OpenWeather API key.
3.Compile the code using gcc -o weather_app weather_app.c -lcurl.
4.Run the application with ./weather_app.
~
